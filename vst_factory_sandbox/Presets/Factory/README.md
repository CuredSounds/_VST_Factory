# Factory presets

Bundled factory presets for the sandbox plugin. Smoke-test baseline lives in
`Tests/PresetSmokeTests/Gain_Baseline.xml` (APVTS export used by CI preset smoke tests).

Add new `.xml` presets here when the plugin grows beyond gain/bypass.
